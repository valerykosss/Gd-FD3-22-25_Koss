// init
const DELAY_MS = 500;
var variable = 1;
let testName = 10;

// if 
// if () { 
// } else if () {

// } else {}

//inf loop + break
for (;;) {
    break;
}

//не меняет массив
[1, 2, 3].forEach(element => {})

//возвращает новый массив
[1, 2, 3].map(element => {})

//true
Boolean([])

//true
Boolean([].length)

Array.from()
[].map()
[].forEach()
[].filter()
[].find()
reduce

rest - запаковывает 
spread - распаковывает 

JSON.parse
JSON.stringify
